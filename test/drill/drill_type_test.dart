
import 'package:fastmath/drill/drill_type.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group("DrillType enum", () {
    test("serializes to correct string values", () {
      // expect(DrillType.trick.toJson(), equals("trick"));
      // expect(DrillType.fast.toString(), equals("fast"));
      // expect(DrillType.challenge.toString(), equals("challenge"));
    });
  });
}